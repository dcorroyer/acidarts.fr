{% extends 'base.html.twig' %}

{% block title project.title %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-md-8">

                </div>
                <div class="col-md-4">
                    <h1>{{ project.title }}</h1>
                    <h4>{{ project.description }}</h4>
                    <a href="#" class="btn btn-primary">Visiter le site web</a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}



